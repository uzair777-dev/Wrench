#!/usr/bin/env bash
set -e
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
echo "Formatting with ruff..."
"${ROOT_DIR}/.venv/bin/ruff" check --fix "${ROOT_DIR}"
echo "Formatting with black..."
"${ROOT_DIR}/.venv/bin/black" "${ROOT_DIR}"
